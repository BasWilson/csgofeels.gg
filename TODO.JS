/*
  - Cant emit to the IO from modules
  - Fix the highRoller issues. Something isnt letting the IO emit to the clients.

  - Slots has an issue where if you get mutiple combos good you will get both.
*/
